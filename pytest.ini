[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
timeout = 60
markers =
    smoke: Quick smoke tests for fast verification
    full: Full test suite (comprehensive)
    benchmark: Performance benchmarks
    reference_node: Extended parity queries from the independent JustyBase Node driver (~727)
    unit: Tests that do not require a live database connection
